import 'package:flutter/material.dart';

const backgroundColor = Colors.black;
const secondaryColor = Color.fromRGBO(20, 20, 20, 1);
var buttonColor = Colors.red[400];
const borderColor = Colors.grey;